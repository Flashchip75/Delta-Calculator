# Linux Bugs

* Wenn von Menü Geometrie auf Path/Velocity gewchselt wird -> Linke Leiste weg und erscheint nicht mehr nach erneutem drücken auf Geometrie.
* In Path Defintions Menü eingabe von Buchstaben möglich, auch wenn ersichtlich nicht in code verarbeitet wird.
* Wenn einmal Calculate full Path (Menü: Path Defenition) gedrück wurde änderung in Menü keine auswirkung in treminal sichtbar - nur alte werde werden angezeigt.
